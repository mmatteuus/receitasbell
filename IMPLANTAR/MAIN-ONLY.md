# MAIN ONLY

Regra obrigatoria.

- Proibido criar novos ramos.
- Todo trabalho deve acontecer no `main`.
- Antes de iniciar qualquer tarefa: `git pull origin main`.
- Depois de cada movimentacao relevante: commit e push.
- O `main` e a unica fonte de verdade.
- O que estiver valido em ramo paralelo deve ser trazido para o `main`.
- Prioridade atual de conversao para o `main`: PR #7 e depois PR #6.
